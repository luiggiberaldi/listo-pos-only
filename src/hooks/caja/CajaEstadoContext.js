import { createContext } from 'react';

/**
 * Contexto para el Estado de Caja (FÉNIX C.9.1)
 * Permite que el estado sea compartido y consistente en toda la app.
 */
export const CajaEstadoContext = createContext(null);